import 'package:flutter/material.dart';

class MyProjectsScreenWidget extends StatelessWidget {
  const MyProjectsScreenWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Column(
      crossAxisAlignment: CrossAxisAlignment.center,
      children: [
        SizedBox(height: 30),
      ],
    );
  }
}
